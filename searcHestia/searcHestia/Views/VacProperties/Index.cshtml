@model IEnumerable<searcHestia.Models.VacProperty>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Location.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MaxOccupancy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VPType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PricePN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Location.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MaxOccupancy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VPType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PricePN)
        </td>
        <td>
            @Html.ActionLink("Photos", "Index", "Galleries", new { vacid = item.Id }, null) |
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            <!-- @Html.ActionLink("Details", "Details", new { id = item.Id }) |  -->
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
